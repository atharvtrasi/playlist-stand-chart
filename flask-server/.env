      
# Spotify API Credentials
# Replace with your actual credentials from the Spotify Developer Dashboard
SPOTIPY_CLIENT_ID=3a08fc29a9c545baa9363c2eb073e789
SPOTIPY_CLIENT_SECRET=6e4c33b8e49b40578dd95bcbd0a34129

# Flask Secret Key (used for session management)
# Replace with a strong, random secret key
SECRET_KEY=spotify_playlist_app_secret_key

# Optional: Flask environment settings for production
FLASK_APP=server.py
FLASK_ENV=production
PYTHONUNBUFFERED=1 # Often helpful for logging in containerized environments

FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:5000
    
    # Optional: Define redirect URI if different from default
    # SPOTIFY_REDIRECT_URI=http://localhost:5000/callback
    # Optional: Set a fixed secret key for Flask sessions
    # FLASK_SECRET_KEY=your_strong_random_secret_key
    # Optional: Set MusicBrainz contact info
    # MB_CONTACT_EMAIL=your_actual_email@example.com
    # MB_APP_NAME=YourAppName
    # MB_APP_VERSION=0.1
    # Optional: Set log level (DEBUG, INFO, WARNING, ERROR)
    # LOG_LEVEL=DEBUG